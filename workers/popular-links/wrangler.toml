name = "popular-links"
main = "src/index.js"
compatibility_date = "2024-11-01"

# This Worker holds a PostHog *personal* API key (read access to project 39273).
# It is NOT in this file — set it as a secret so it never lands in git:
#
#   npx wrangler secret put POSTHOG_API_KEY
#
# For local dev (`npx wrangler dev`) put it in workers/popular-links/.dev.vars:
#
#   POSTHOG_API_KEY=phx_xxxxxxxx
#
# (.dev.vars is gitignored.)
